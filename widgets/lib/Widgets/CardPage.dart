import 'package:flutter/material.dart';
import 'WidgetPage.dart';

class CardPage extends WidgetPage{
  const CardPage({super.key});
  
  @override
  Widget build(BuildContext context) {
     return super.build(context);
  }

}